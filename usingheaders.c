#include <stdio.h>
#include "exampleheader.h"

main()
{
    float radius;
    printf(Bee);
    printf("\n pick a radius: \n");
    scanf(" %f", &radius);
    printf("The circumference of a circle with that radius would be around %.2f.\n", radius*2*PI);
}
